# ChartAdapter

## Build
Run `npm i` to install dependencies

Run `ng build chart-adapter --configuration development` to build chart-adapter library.
## Run
Run `npm run start` to start the application
